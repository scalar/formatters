import { format } from './format'
import { formatSync } from './format-sync'

export type { FormatOptions, FormatResult, PhpFormatterRuntime, Rules } from './types'
export { format, formatSync }

export default format
